# EG1 Public Note

Gate: `MP_G1`
Constant: `kappa_exponential`

This note isolates the active response package. The projected response operator is ``X_tau = Pi_resp L_tau Pi_resp`.`. The gate closes once the admissible tube carries a uniform positive floor on the projected response sector.
